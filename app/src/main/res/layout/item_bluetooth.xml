<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:id="@+id/dl_bluetooth_item"
    android:padding="10dp">

    <TextView
        android:id="@+id/dl_bluetooth_device_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="기기 이름"
        android:textColor="@color/black"
        android:textSize="16dp"
        android:fontFamily="@font/main_font"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <TextView
        android:id="@+id/dl_bluetooth_not_connect_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="연결안됨"
        android:textSize="13dp"
        android:textColor="@color/placeholder_gray"
        android:fontFamily="@font/main_font"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="@id/dl_bluetooth_device_tv"
        app:layout_constraintTop_toBottomOf="@id/dl_bluetooth_device_tv"/>

    <TextView
        android:id="@+id/dl_bluetooth_connect_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="연결"
        android:textSize="13dp"
        android:textColor="@color/placeholder_gray"
        android:fontFamily="@font/main_font"
        app:layout_constraintStart_toStartOf="@id/dl_bluetooth_device_tv"
        app:layout_constraintTop_toBottomOf="@id/dl_bluetooth_device_tv"/>

</androidx.constraintlayout.widget.ConstraintLayout>